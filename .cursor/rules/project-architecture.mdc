---
description:
globs:
alwaysApply: false
---
# Edu Platform - Архитектура проекта

## Режим работы: PLAN → ACT

При работе с проектом всегда следуйте двухэтапному подходу:

### 🔍 PLAN (Планирование)
1. **Анализ требований** - четко понимайте задачу
2. **Архитектурное планирование** - определите компоненты и их взаимодействие
3. **Выбор технологий** - используйте подходящие инструменты из стека
4. **Планирование изменений** - определите что и где изменить
5. **Оценка рисков** - предвидите потенциальные проблемы

### ⚡ ACT (Действие)
1. **Реализация** - пишите качественный код согласно плану
2. **Тестирование** - проверяйте функциональность
3. **Документирование** - добавляйте комментарии и документацию
4. **Рефакторинг** - улучшайте код при необходимости

## Архитектура проекта

### Микросервисная архитектура
- **Frontend**: Next.js 15 + React 19 + TypeScript
- **Backend**: FastAPI микросервисы (Python)
- **Database**: PostgreSQL
- **Orchestration**: Docker Compose
- **Reverse Proxy**: Nginx

### Структура проекта
```
edu-platform/
├── web_app/                 # Next.js frontend
│   ├── src/
│   │   ├── app/            # App Router pages
│   │   ├── components/     # React components
│   │   ├── services/       # API services
│   │   ├── stores/         # Zustand stores
│   │   └── types/          # TypeScript types
├── services/               # Backend микросервисы
│   ├── auth_service/       # Аутентификация
│   ├── results_service/    # Результаты тестов
│   ├── quiz_service/       # Управление тестами
│   └── shared/            # Общие компоненты
└── docker-compose.yml     # Оркестрация
```

## Принципы разработки

### Frontend (Next.js)
- Используйте [App Router](mdc:web_app/src/app/)
- Компоненты в [components/](mdc:web_app/src/components/)
- Состояние через [Zustand stores](mdc:web_app/src/stores/)
- API вызовы через [services/](mdc:web_app/src/services/)
- Типизация в [types/](mdc:web_app/src/types/)

### Backend (FastAPI)
- Структура сервиса: `app/` → `api/` → `services/` → `models/`
- Используйте [SQLAlchemy](mdc:services/auth_service/app/models.py) для ORM
- [Alembic](mdc:services/auth_service/alembic.ini) для миграций
- [Pydantic](mdc:services/auth_service/app/schemas.py) для валидации

### База данных
- PostgreSQL с асинхронным подключением
- Миграции через Alembic
- Схемы в [models.py](mdc:services/auth_service/app/models.py)

## Команды разработки

### Запуск проекта
```bash
# Полный запуск
make build

# Разработка
make dev

# Логи
make logs
```

### Миграции
```bash
# Auth service
make auth_service_migrate
make auth_service_upgrade

# Results service  
make results_service_migrate
make results_service_upgrade

# Quiz service
make quiz_service_migrate
make quiz_service_upgrade
```

## Стандарты кода

### TypeScript/JavaScript
- Строгая типизация
- ESLint + Prettier
- Компоненты в PascalCase
- Файлы в kebab-case

### Python
- Black для форматирования
- Type hints обязательны
- Docstrings для функций
- PEP 8 стандарты

### Общие принципы
- Чистая архитектура
- SOLID принципы
- DRY (Don't Repeat Yourself)
- KISS (Keep It Simple, Stupid)
